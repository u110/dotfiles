# prefix key (Ctl-a -> Ctl-z)
escape ^z^z

# 現在のscreenリストを表示
bind l windowlist

# 画面下に時刻＆スクリーン一覧を表示する。
hardstatus alwayslastline "[%02c] %`%-w%{=b bw}%n %t%{-}%+w"

# status
shelltitle "$ |bash"

# 画面点滅をオフ
vbell off

